// programa usado para ver se o aluno foi aprovado ou não foi aprovado.
// programa usa o if e else para ver se o aluno foi aprovado ou não foi aprovado.
// código é simples e fácil de entender.

void main(){
  double nota = 7.0;
  if (nota >= 5.0) {
    print("Aprovado !!!");
  } else {
    print("Reprovado");
  }
}